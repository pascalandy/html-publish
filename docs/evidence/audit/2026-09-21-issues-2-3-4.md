# Acceptance audit: issues #2, #3, #4

Audited 2026-09-22 against immutable runtime commit
cc6ceea669395b02d1f061319a1a05d9fb52f56c. The runtime is unchanged at reviewed head
57ff87909051729919f05ae8c2db36a421217e6c, where `just check` ran green with all 89 tests. Older
hosting records keep their original provenance. Each verdict cites a test name, a file, or a
recorded evidence artifact. Verdicts:

- `passes` means executable or recorded evidence exists and shows the criterion holds.
- `partial` means evidence exists but does not cover the full criterion; the missing part is
  named.
- `missing` means the criterion can be tested locally, but no evidence exists at this commit.
- `open` means the evidence needs access this environment does not have. The needed access is
  named.

The audit created no behavior changes. Local gaps remain for normalized archive modes and the
parentless first commit. Some interruption boundaries also remain qualified below. Gaps that need
`om1`, a second tailnet device, or a browser session stay open and are listed at the end.

## Issue #2, private hosting proof

Evidence base: [docs/evidence/hosting/2026-09-21-om1-mvp.md](../hosting/2026-09-21-om1-mvp.md)
and [docs/evidence/deployment/2026-09-21-om1-controlled-mvp.md](../deployment/2026-09-21-om1-controlled-mvp.md).

| Criterion | Verdict | Evidence |
| --- | --- | --- |
| Record Tailscale version, Serve configuration, HTTPS/TCP routes, existing endpoint responses, and prefix ownership before changes | partial | Hosting doc Baseline records Tailscale 1.102.3, Git 2.55.0, HTTPS 443, 8443, TCP 5173, and states port 8444 was unused and the full Serve configuration was saved. The saved configuration itself is not committed, and ownership of the `/html-publish` prefix is not stated beyond the probe choice. |
| Probe an unused scoped temporary route: `index.html`, HTML content type, redirects, relative assets, controlled symlinks | partial | Hosting doc records directory 200 text/html, relative CSS 200 text/css, and missing 404 through `--set-path /html-publish-probe`. Trailing-slash redirect and explicit controlled-symlink rows are absent from the results table. |
| Atomic selection from private staging on one filesystem; concurrent requests see complete files | passes | Hosting doc records 300 concurrent requests producing 139 complete A bodies and 161 complete B bodies with zero partial bodies. Not re-verified here; needs `om1`. `test_future_public_directory_can_appear_after_server_start` covers the selection-after-start analog at unit level. |
| Browser caching: same-size changes, forced equal timestamps, A to B to A restore, replayed validators, recorded headers and browser observations | partial | Hosting doc records an equal-timestamp update where an old `If-Modified-Since` request received a false 304 from the tested Python loopback proxy. It also records an unconditional A to B to A pass. The validator value was not recorded. `If-None-Match` was not replayed. No browser observation exists for this probe. The repository server behavior is proven by `test_same_size_same_mtime_update_ignores_old_conditional_request` and `test_redirect_and_errors_disable_caching_and_directory_listing`. |
| Deleted assets, file/directory transitions, nonexistent path, Unicode, space, and percent filenames, mount-boundary redirects; missing must not fall through to an app | partial | Missing path 404 recorded in the hosting doc; deleted-asset 404 proven by `test_guarded_update_replaces_complete_site_and_preserves_other_page`; directory-listing 404 proven by `test_redirect_and_errors_disable_caching_and_directory_listing`. File/directory transitions, Unicode, space, percent, and mount-boundary cases are unprobed at the hosting layer. |
| Git, staging, receipts, temporary aliases, and outside paths not fetchable, including encoded traversal; service permissions inspected | partial | `test_mime_types_are_preserved_and_uncontrolled_symlinks_are_hidden` proves outside symlinks 404 and `test_redirect_and_errors_disable_caching_and_directory_listing` proves directory listings 404 on the controlled server. Encoded traversal attempts against the real route and service-permission inspection beyond the recorded anecdote are missing. |
| Select native Serve only if every behavior passes; otherwise document the failure and prove one loopback Caddy alternative with the identical cases rerun | open | Direct Tailscale file serving was not tested because the user lacked permission to add a filesystem-backed handler. The false 304 rejects only the tested Python loopback proxy. An authorized isolated test must test direct Serve first. If direct Serve fails the same cases, test one loopback Caddy configuration. |
| Reproducible probe script and results under `docs/evidence/hosting/` with versions, commands, expected and observed results, selected configuration, and unresolved checks | partial | The results document has versions, commands, and observed results. No probe script or selected production configuration exists. Direct Serve remains untested, and the Caddy alternative is conditional on that result. |
| Real authorized tailnet-client evidence; loopback is not sufficient | passes | Hosting doc records a second authorized tailnet device receiving 200 with the same SHA-256 body hash. Re-verification needs the device. |
| Remove only owned probe routes and files, compare configuration again, recheck original endpoints; stop if configuration changed concurrently | passes | Hosting doc records scoped removal of the owned 8444 route, an exact match of the normalized configuration against the saved baseline, and matching original endpoints afterward. |

## Issue #3, core, protocol, first publication

Evidence base: `tests/test_cli.py`, `tests/test_server.py`, `tests/test_remote.py`,
`tests/test_deploy.py`, and the modules under `html_publish/`.

| Criterion | Verdict | Evidence |
| --- | --- | --- |
| Single Python/stdlib CLI plus Git, small internal modules, no plugin framework or server, executable help, supported version checks | partial | `artifact.py`, `store.py`, `delivery.py`, `model.py`, and private `_git.py` implement capture, archive, activation, and observation with zero runtime dependencies. `_git.check_supported_version` requires Git 2.36, the first version that supports both `git init --object-format` and the configured `core.fsync` settings. `test_git_older_than_the_supported_minimum_is_rejected` and `test_a_missing_git_executable_is_reported_as_unavailable` prove rejection and error behavior. `test_git_at_the_supported_minimum_can_publish` proves boundary-string parsing and dispatch to Git, while the [real Git 2.36 loopback proof](2026-09-22-pr14-git-2.36-boundary.md) proves compatibility with that release. `test_help_documents_the_commands` proves the executable help. The 89f078e baseline also exports a server, remote client, and installer as described below, so the ownership criterion remains partial. |
| Versioned JSON, stable errors and next actions, exit statuses 0/1/2, partial effects, bounded output, timeout behavior, caught JSON usage errors, one stdout contract | passes | `test_json_usage_error_uses_exit_two_and_one_object` asserts `schema_version`, exit 2, one object, and empty stderr. Conflict tests prove exit 1. `test_delivery_failure_reports_selected_unverified_state` and `test_saved_update_is_reused_when_activation_has_not_happened` prove partial effects. `test_total_deadline_interrupts_a_trickling_http_body` and `test_git_preflight_uses_the_total_command_deadline` prove that HTTP and Git preflight consume the same total command deadline. `test_lock_timeout_conflicts_without_mutation`, `test_a_broken_archive_path_reports_archive_failure`, and `test_byte_level_release_corruption_is_refused_then_recoverable` prove representative error codes. Deeper fault codes land with the recovery ticket. |
| `plan`, read-only `status`, and the first `publish`; plan uses temporary storage; expectations verified before durable writes; missing revisions stay null | passes | `test_plan_is_advisory_and_does_not_create_persistent_state`, `test_plan_accepts_existing_empty_bare_archive_without_writing`, and `test_status_reports_saved_and_selected_facts_without_http` prove the three commands. `test_plan_reports_configured_capture_limits` proves the limits echo, `test_plan_reports_capture_warnings` the warning codes, and `test_status_of_an_absent_name_observes_nulls` the absent-name nulls. |
| Capture of one file or a directory with the S3 unsafe-input rejection matrix | passes | `test_capture_rejects_unsafe_inputs_without_persistent_state` proves dot-prefixed, backslash, control, delete, invalid-encoding, special-file, and missing-index rejection. `test_capture_limit_violations_report_which_limit_was_hit` proves both limits, and the recovery change proves source-change fault injection with `test_source_change_during_capture_fails_without_persistent_state`. The rejection test found and fixed a real crash: an invalid encoding used to abort with a traceback before the structured error. |
| Bare archive with `<name>/site/` trees, normalized modes, parentless first commit, conditional ref advancement, neutralized Git environment | partial | `test_directory_assets_and_second_name_preserve_both_pages` asserts the combined tree paths and released bytes for two names. `test_git_filters_and_excludes_do_not_change_artifact_identity` asserts the complete archived path set and exact archived, released, and served bytes under hostile Git filters and excludes. `test_conditional_ref_failure_leaves_the_proposed_commit_unreferenced` proves conditional ref advancement. No test asserts mode `100644` or that the first commit has no parent. |
| Export committed bytes, validate membership and bytes, immutable release, privately staged symlink, filesystem boundaries, no intermediates or hardlinks | partial | `test_git_filters_and_excludes_do_not_change_artifact_identity` asserts archived, released, and served bytes. `test_saved_update_is_reused_when_activation_has_not_happened` proves staged activation and refusal, and `test_byte_level_release_corruption_is_refused_then_recoverable` proves byte-level refusal. `test_rename_failure_reports_export_failure_with_stage_usage` proves a failed export rename reports `export_failure`, preserves a nonempty staging directory, and does not activate. `test_kill_after_export_completion_reuses_the_validated_export` proves retry after ordinary child termination reuses the completed export. `test_fsync_failure_after_selection_reports_persistence_failure` proves a post-selection directory-sync failure reports `persistence_failure` with activation recorded. No test simulates a write interruption, an external `SIGKILL`, or power loss. The no-intermediate and no-hardlink subcriteria remain unasserted. |
| HTTP verification: directory URL, HTML type, all files, missing sentinel, redirect bounds, identity bodies, budget; failure after activation reports the selected revision | passes | `test_delivery_failure_reports_selected_unverified_state` proves the failure report and `test_publish_file_and_identical_retry_use_one_commit` the plain stable URL. `test_redirect_outside_the_publication_boundary_is_rejected` and `test_a_redirect_loop_exceeds_the_hop_limit` prove redirect bounds, and `test_a_compressed_body_fails_byte_comparison` proves identity-encoded byte comparison. |
| Real CLI against temporary directories and controlled HTTP; executable evidence for first publication, unsafe input, wrong target, failures, timeouts, Git transformations, source immutability | passes | `test_cli.py` drives `python -m html_publish` against a loopback server. `test_git_filters_and_excludes_do_not_change_artifact_identity` proves Git-transformation immunity, and `test_publishing_leaves_the_source_untouched` proves source immutability. First publication, unsafe input, wrong target, archive failure, and timeout have their own tests. |
| Authoritative contract moves to `docs/contract.md`; short `AGENTS.md` map | passes | `docs/contract.md` is added by the change that carries this audit. `AGENTS.md` is a short map. |
| Executable and contract tests own exact syntax; skills do not duplicate the publisher | passes | No skills existed at the 89f078e baseline. The later `verify-html-publish` skill drives the executable and loopback server, while `test_stop_refuses_stale_identity_and_preserves_the_instance` proves its cleanup helper refuses a stale process identity. `AGENTS.md` links the skill. |

### Baseline departures from contract ownership

At commit 89f078e, `pyproject.toml` exports `html-publish-server`, `html-publish-remote`, and
`html-publish-deploy`. The installer in `html_publish/deploy.py` starts `html_publish.server` as
the persistent HTTP backend. The [README](../../../README.md) and the
[controlled operations guide](../../operations.md) document the remote client and installer as
the operating route.

The core transaction ownership still matches the contract. `html_publish/store.py` owns archive
and activation mutations, and `html_publish/cli.py` owns CLI arguments, JSON, and exit codes. The
delivery and installation owners do not match S1, S8, and S10. Those sections place HTTP delivery
in native Serve or a verified Caddy alternative, prohibit a remote adapter or daemon, and exclude
a persistent publisher. A later architecture decision must reconcile the implementation and the
target contract. Adding tests does not resolve this departure. This audit does not classify the
existing controlled deployment as a finished-product defect or remove its tools.

## Issue #4, guarded transitions

| Criterion | Verdict | Evidence |
| --- | --- | --- |
| S5 rules in order: degraded rejection, verified identical no-op before expectation check, guarded creation, exact expectation match | passes | The order is implemented in `_decide_publication`. `test_guarded_update_replaces_complete_site_and_preserves_other_page` proves the no-op precedence, `test_stale_expected_revision_conflicts_without_mutation` proves exact matching, and `test_degraded_selection_blocks_publication_until_repair` proves degraded rejection blocks both a repeat and an expected update until the link is repaired. |
| Create/retry semantics distinct from expectation semantics; corrupt state never treated as absence | passes | `test_absent_active_content_follows_the_saved_and_expectation_rules` proves identical-saved resumption without an expectation, saved-content conflict, and expectation-against-absent conflict, with exactly one commit across all three. `test_degraded_selection_blocks_publication_until_repair` proves degraded state is never treated as absence. |
| No-op with a stale expectation changes nothing even when a different revision is saved but inactive; report the pending archive | passes | `test_noop_with_a_stale_expectation_reports_the_pending_archive` proves the no-op keeps the branch and selection untouched, keeps the saved-but-inactive revision, and reports it as `archived_revision` while the stale expectation is ignored. |
| Content identity: B to A requires expectation B; a later expectation of A is valid when A is active again; no epochs | passes | `test_content_identity_permits_returning_to_an_earlier_revision` replays A to B to A to B with expectations and asserts four commits, two distinct content revisions, and one stable URL. |
| Reuse identical saved content; otherwise append a commit preserving other names; conditional ref failure leaves the proposed commit unreferenced | passes | Reuse without a new commit is asserted in `test_absent_active_content_follows_the_saved_and_expectation_rules` including the single release directory. Preservation of other pages is asserted by the guarded-update tree checks. `test_conditional_ref_failure_leaves_the_proposed_commit_unreferenced` proves a rival external ref move wins and the proposed commit stays reachable to no branch. |
| One bounded lock through verification; real concurrent processes; children retain no locks | partial | `test_two_updates_from_one_revision_yield_one_publish_and_one_conflict` proves differing same-name concurrency, `test_concurrent_identical_publications_converge` proves identical convergence without duplicate history, `test_concurrent_publications_of_different_names_both_succeed` proves different-name concurrency, and `test_lock_timeout_conflicts_without_mutation` proves the bounded wait. The recovery tests `test_kill_before_ref_advancement_recovers_by_retry`, `test_kill_after_ref_advancement_reuses_the_saved_archive`, `test_kill_after_export_completion_reuses_the_validated_export`, and `test_kill_after_selection_leaves_active_but_unverified_content` terminate a child with `os._exit(9)` and then complete a retry, proving that ordinary process termination releases the process-held lock at those boundaries. External `SIGKILL`, reboot, and power loss remain untested. |
| `plan` shares the guard evaluator with bounded added, changed, and deleted paths including transitions | passes | Prediction and differences are tested for create, update, unchanged, and conflict. `test_file_and_directory_transitions_appear_in_plan_differences` publishes `assets` as a file, replaces it with `assets/style.css`, then restores the file. It asserts both plan differences, both archived path sets, the served replacement bytes, and the removed nested path's 404. |
| Stable URL, removed assets, same-size changes, Git filter and exclude immunity, healthy export reuse, corruption refusal, distinguishable revisions in JSON | passes | `test_guarded_update_replaces_complete_site_and_preserves_other_page` proves the stable URL, removed-asset 404, and revision distinction. `test_same_size_update_changes_the_served_bytes` proves same-size content changes. `test_git_filters_and_excludes_do_not_change_artifact_identity` configures hostile global and repository filters and excludes, then asserts the complete archived path set plus exact archived, released, and served bytes. `test_byte_level_release_corruption_is_refused_then_recoverable` proves byte-level corruption refusal and healthy reuse after repair. |

## What remains, by owner

The merged PR 14 evidence and this recovery change leave two named local archive gaps: normalized
mode `100644` and a parentless first commit. The export row also retains its named interruption,
intermediate-path, and hardlink gaps. The lock row proves recovery only after ordinary child
termination. Reconciling the server, remote client, and installer with the contract requires a
separate architecture decision. Live hosting gaps remain outside local test coverage.

These need access outside this environment and keep issue #2 open:

- An authorized isolated direct Tailscale file-serving test. If it fails the required freshness
  cases, prove one loopback Caddy alternative with the same cases.
- `If-None-Match` replay, Unicode, space, percent, mount-boundary, deleted-asset, and
  file/directory-transition rows in the hosting probe.
- A reusable probe script under `docs/evidence/hosting/`.
- Browser observations for the freshness rows, a second tailnet device, and re-verification of
  the recorded atomic-selection and cleanup rows.

All of those belong to the next operator session on `om1`. Until then, issue #2 stays open and
this MVP is not production-ready.
